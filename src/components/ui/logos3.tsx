'use client';

import { motion } from 'framer-motion';
interface Logo {
  src: string;
  alt: string;
}
interface Logos3Props {
  heading?: string;
  logos: Logo[];
}
export const Logos3 = ({ heading, logos }: Logos3Props) => {
  return <div></div>;
};
